# Generated reports

`figures/` stores publication-quality plots and `tables/` stores metrics plus out-of-fold
predictions. Generated results are ignored until deliberately selected for a tagged release. Never
publish a summary metric without cell- and horizon-level evidence.
