# Model artifacts

Serialized estimators and preprocessing pipelines belong here and are ignored by Git. A released
model must have a matching configuration, dataset checksum, outer-fold predictions, metrics,
package versions, and completed `docs/model_card.md`.
